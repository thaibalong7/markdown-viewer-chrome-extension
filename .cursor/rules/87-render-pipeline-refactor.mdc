---
description: Render pipeline refactor guardrails
globs: src/viewer/core/**/*.js,src/viewer/app/renderController.js,src/plugins/**/*.js
alwaysApply: false
---

# Render Pipeline Refactor

- Do not add any rendered HTML path that bypasses `sanitizeHtml()`.
- Keep `renderDocument()` as the main markdown -> safe HTML boundary; callers should receive sanitized HTML before `renderIntoElement()`.
- React owns the viewer shell only. It must not manage or reconcile the rendered article DOM under `.mdp-markdown-body`.
- Shiki theme changes, reader theme changes that affect code colors, and plugin enable/config changes must preserve current render behavior.
- Extract pure parsing or normalization logic before changing orchestration. Cover edge cases with focused tests.
- Cache or reuse render context only when invalidation by plugin/parser/theme-affecting settings is explicit and tested.
- Do not cache unsafe pre-sanitize HTML across the sanitize boundary unless the cache key and sanitize step remain clear.
